// Single place to point the frontend at the backend API. Edit this for
// each deployment target (see docs/08-deployment.md).
window.MEDITRACK_API_BASE = window.MEDITRACK_API_BASE || 'http://localhost:8000/api';
